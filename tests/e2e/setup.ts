import { beforeAll, afterAll, beforeEach, afterEach } from 'vitest';

beforeAll(async () => {
  // Prepare global E2E resources if needed
});

afterAll(async () => {
  // Cleanup global E2E resources
});

beforeEach(async () => {
  // Launch isolated browser context
});

afterEach(async () => {
  // Teardown browser context and fully disconnect CDP
});
